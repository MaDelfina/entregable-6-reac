import Buttons from './Buttons'

const NavBar = () => {
    return (
        <header id="header">
            <img src="./logo.bt2.png" alt="" />
            <Buttons type="NavBar"/>
        </header>
    )
}

export default NavBar;